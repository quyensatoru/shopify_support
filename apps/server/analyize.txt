You are a Shopify embedded app support engineer performing root cause analysis.
LANGUAGE RULE: Detect the language of the Issue text and write ALL text output fields in that same language. Do not translate code identifiers, file paths, or technical names.

App: mida
Issue: Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 
Case type: data_integrity

=== CODEBASE CONTEXT ===
--- Repo: sama-proxy (express) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **pingRouter** (constant) - src/routes/ping.route.js:8
  `= new Router({
    prefix: '/recorder',
})`

### Code

#### pingRouter (src/routes/ping.route.js:8)

```javascript
export const pingRouter = new Router({
    prefix: '/recorder',
});
```


### ⚠️ Low-confidence match

This query matched mostly on common words, so the entry points above may be off-target — treat them as a starting point, not a complete answer. For a reliable result:
- `codegraph_explore` with the **exact symbol names** you are after (class / function / method names), or
- `codegraph_search <name>` for one specific symbol
- `codegraph_files` a likely area: `src/routes`

Do not assume the list above is comprehensive.
Relevant symbols:
  constant app @ src/server.js:14
  import koa @ src/server.js:5
  import koa-proxies @ src/routes/event.route.js:2
  import koa-static @ src/server.js:6
  import @koa/router @ src/routes/api.route.js:1
  import koa-proxies @ src/routes/api.route.js:2
  import @koa/router @ src/routes/crm.route.js:1
  import koa-proxies @ src/routes/crm.route.js:3
  import @koa/router @ src/routes/event.route.js:1
  import @koa/router @ src/routes/hm.route.js:1

--- Repo: sama-extensions (express) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **record** (function) - scripts/rrweb/record/index.js:21
  `(options = {})`
- **checkoutStartedHandler** (function) - extensions/web-pixel/src/events /checkout_started.js:4
  `({ event, init, settings, browser })`
- **checkoutCompletedHandler** (function) - extensions/web-pixel/src/events /checkout_completed.js:22
  `({ event, init, settings, browser })`

### Related Symbols

- extensions/web-pixel/src/helpers/application-store.helper.js: loadVisitor:67, loadSession:45, loadPage:79, getCartInfo:136
- extensions/web-pixel/src/helpers/send_event.js: createPingQuery:4, sendEvent:22
- extensions/web-pixel/src/events /checkout_completed.js: groupProductOrdered:4
- scripts/rrweb/record/error-handler.js: registerErrorHandler:2
- scripts/rrweb/rrweb-snapshot/utils.js: reset:123
- scripts/rrweb/utils.js: polyfill:223

### Code

#### record (scripts/rrweb/record/index.js:21)

```javascript
function record(options = {}) {
    // mr_00002
    window.msrRecord = {
        externalCDNs: []
    }
    const { emit, checkoutEveryNms, checkoutEveryNth, blockClass = 'rr-block', blockSelector = null, ignoreClass = 'rr-ignore', ignoreSelector = null, maskTextClass = 'rr-mask', maskTextSelector = null, inlineStylesheet = true, maskAllInputs, maskInputOptions: _maskInputOptions, slimDOMOptions: _slimDOMOptions, maskInputFn, maskTextFn, ho
Relevant symbols:
  method setupPeriodicSnapshot @ scripts/rrweb/record/dynamic-dom-handler.js:267
  import koa @ server/index.js:1
  import koa-static @ server/index.js:2
  constant app @ server/index.js:5

--- Repo: sama-recorder (express) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **checkSearchEngines** (function) - helpers/session.helper.js:28
  `(url)`
- **checkIpIncludeCIDRRange** (function) - helpers/ipAddress.helper.js:8
  `(ipAddress, addressCIDR)`
- **checkIpExcludeInArrayCIDRRange** (function) - helpers/ipAddress.helper.js:16
  `(arrayCIDRRange, ip)`

### Related Symbols

- helpers/ipAddress.helper.js: ipRangeCheck:1

### Code

#### checkSearchEngines (helpers/session.helper.js:28)

```javascript
const checkSearchEngines = (url) => {
    for (const searchEngine of SEARCH_ENGINES) {
        if (url && url.startsWith(searchEngine)) {
            return true;
        }
    }
    return false;
};
```

#### checkIpIncludeCIDRRange (helpers/ipAddress.helper.js:8)

```javascript
function checkIpIncludeCIDRRange(ipAddress, addressCIDR) {
    if (!IPCIDR.isValidCIDR(addressCIDR)) {
        return false;
    } else {
        return ipRangeCheck(ipAddress, addressCIDR);
    }
}
```

#### checkIpExcludeInArrayCIDRRange (helpers/ipAddress.helper.js:16)

```javascript
function checkIpExcludeInArrayCIDRRange(arrayCIDRRange, ip) {
    if (arrayCIDRRange && arrayCIDRRange.length > 0) {
        let isResult = false;
        for (let i = 0; i < arrayCIDRRange.length; i++) {
            if (checkIpIncludeCIDRRange(ip, arrayCIDRRange[i])) {
                isResult = true;
                break;
         
Relevant symbols:
  constant Koa @ server.js:3
  constant koaBody @ server.js:5
  constant app @ server.js:16
  constant Router @ routes/index.js:1
  constant Router @ routes/session.route.js:1
  constant cors @ server.js:7

--- Repo: sama-search (express) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **POST /top_paid_traffic_sources** (route) - src/routes/session.route.js:9
- **ABANDONED_CHECKOUT** (constant) - src/constant/session.js:51
  `= 'abandoned-checkout'`

### Code

#### POST /top_paid_traffic_sources (src/routes/session.route.js:9)

```javascript
router.post("/top_paid_traffic_sources", SessionController.topPaidTrafficSources);
```

#### ABANDONED_CHECKOUT (src/constant/session.js:51)

```javascript
const ABANDONED_CHECKOUT = 'abandoned-checkout';
```


### ⚠️ Low-confidence match

This query matched mostly on common words, so the entry points above may be off-target — treat them as a starting point, not a complete answer. For a reliable result:
- `codegraph_explore` with the **exact symbol names** you are after (class / function / method names), or
- `codegraph_search <name>` for one specific symbol
- `codegraph_files` a likely area: `src/routes`, `src/constant`

Do not assume the list above is comprehensive.

--- Repo: sama-hm (express) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **CHECKOUT** (constant) - src/helpers/event.helper.js:8
  `= 'checkout-begin'`
- **PURCHASE** (constant) - src/helpers/event.helper.js:7
  `= 'checkout'`
- **CONVERSION_TYPES** (constant) - src/models/heatmap/click.heatmap.js:5
  `= [
    'add-to-cart',
    'begin_checkout',
    'abandoned_checkout',
    'abandoned_cart',
    'chec...`

### Code

#### CHECKOUT (src/helpers/event.helper.js:8)

```javascript
const CHECKOUT = 'checkout-begin';
```

#### PURCHASE (src/helpers/event.helper.js:7)

```javascript
const PURCHASE = 'checkout';
```

#### CONVERSION_TYPES (src/models/heatmap/click.heatmap.js:5)

```javascript
const CONVERSION_TYPES = [
    'add-to-cart',
    'begin_checkout',
    'abandoned_checkout',
    'abandoned_cart',
    'checkout',
    'sign_up',
    'sign_in',
    'newsletter_subscribe',
    'contact_form',
    'custom_event',
];
```


### ⚠️ Low-confidence match

This query matched mostly on common words, so the entry points above may be off-target — treat them as a starting point, not a complete answer. For a reliable result:
- `codegraph_explore` with the **exact symbol names** you are after (class / function / method names), or
- `codegraph_search <name>` for one specific symbol
- `codegraph_files` a likely area: `src/helpers`, `src/models/heatmap`

Do not assume the list above is comprehensive.
Relevant symbols:
  constant Koa @ src/server.js:3
  constant koaBody @ src/server.js:5
  constant koaCompress @ src/server.js:6
  constant app @ src/server.js:13
  constant Router @ src/routes/area.route.js:1
  constant Router @ src/routes/click.route.js:1
  constant Router @ src/routes/metrics.route.js:1
  constant Router @ src/routes/move.route.js:1
  constant Router @ src/routes/page.route.js:1
  constant Router @ src/routes/scroll.route.js:1

--- Repo: sama-api (express) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **checkShop** (function) - custom_script/subscription/check_differ_shop.js:14
  `(shop)`
- **checkDue** (function) - controllers/internal/config/review.controller.js:7
  `(ctx)`
- **RecordChannel** (constant) - queues/channel/internal/record.channel.js:5
  `= (function () {
    let channel;

    const initialize = async (conn) => {
        try {
            ...`

### Related Symbols

- custom_script/subscription/check_differ_shop.js: processBatch:46

### Code

#### checkShop (custom_script/subscription/check_differ_shop.js:14)

```javascript
async function checkShop(shop) {
    const { domain, access_token, charge_id, subscription_info } = shop;

    if (!access_token || !charge_id || !subscription_info?.title) {
        return null;
    }

    try {
        const charge = await getActualPlanFromShopify(domain, access_token, charge_id);
        const dbSubPlan = subscription_info.title.trim().toLowerCase();
        const realSubPlan = charge.name.trim().toLowerCase();

        const isSamePlan = dbSubPlan.includes(realSubPlan) || realSubPlan.includes(dbSubPlan);
        const isCancelled = charge.status === 'cancelled';

        if ((isCancelled && isSamePlan) || !isSamePlan) {
            return {
                domain,
                charge_id,
                chare_status: charge.status,
                db_plan
Relevant symbols:
  constant Koa @ server.js:5
  constant koaBody @ server.js:6
  constant koaCompress @ server.js:7
  constant koaStatic @ server.js:8
  constant app @ server.js:20
  constant ratelimit @ middleware/rateLimit.middleware.js:1
  constant Router @ routes/external/cms-plan.route.js:1
  constant Router @ routes/external/cms-setting.route.js:1
  constant Router @ routes/external/cms-shop-session.route.js:1
  constant Router @ routes/external/crm.route.js:1

--- Repo: sama-cms (react) ---
## Code Context

**Query:** Khách báo ko có record trong ngày hôm nay. check xem nguyên nhân do đâu sao lại ko có traffic trong ngày hôm nay 

### Entry Points

- **recorderSlice** (constant) - web/frontend/redux/reducers/recorder.reducer.js:34
  `= createSlice({
    name: 'recorder',
    initialState,
    reducers: {
        setChangeIndexReplay: ...`
- **CheckoutOverlay** (component) - packages/replayer/src/components/CheckoutOverlay.svelte:1
- **recorderActions** (constant) - web/frontend/redux/reducers/recorder.reducer.js:122
  `= recorderSlice.actions`

### Related Symbols

- packages/replayer/src/components/CheckoutOverlay.svelte: checkoutStyle:4, onUpgrade:5, onShopifyPlus:6, isUpgrade:7, isShopifyPlus:8
- packages/replayer/src/components/Player.svelte: Player:1
- web/frontend/components/HeatmapV2/FrameWorker/index.jsx: HeatMapFrameWorker:47

### Code

#### recorderSlice (web/frontend/redux/reducers/recorder.reducer.js:34)

```javascript
export const recorderSlice = createSlice({
    name: 'recorder',
    initialState,
    reducers: {
        setChangeIndexReplay: (state, action) => {
            state.changeIndexReplay = action.payload;
        },
        setPagination: (state, action) => {
            for (let [key, value] of Object.entries(action.payload)) {
                state.pagination[key] = value;
                if (typeof value === 'object' && !Array.isArray(value) && value !== null) {
                    for (let [keyNested, valueNested] of Object.entries(
===

Hypotheses to evaluate:
[be5283aa-7d74-4b36-9a0c-4860bb927499] rank=1: Frontend không gửi ping/record request tới server do lỗi cấu hình hoặc lỗi mạng, dẫn tới không có traffic ghi lại.
  CONFIRM: Không có bản ghi mới trong collection recordings của recorder-db, Log server không nhận request /recorder/ping từ frontend
  REJECT: Có bản ghi mới trong DB, Log cho thấy request tới endpoint /recorder/ping thành công

[039ab229-0688-443c-8af9-a3dc6432c20a] rank=2: Pipeline xử lý dữ liệu (RabbitMQ → Redis → Mongo) bị ngưng hoặc lỗi, khiến dữ liệu không được lưu.
  CONFIRM: Queue RabbitMQ không nhận message mới, Log cho thấy lỗi khi ghi vào Redis/cache
  REJECT: Queue hoạt động bình thường, không có lỗi kết nối, Redis ghi thành công

[c4945c74-619e-44db-bb38-07a9560fcdea] rank=3: Kết nối với Shopify bị mất (OAuth token hết hạn), do đó không nhận được sự kiện traffic.
  CONFIRM: Shopify webhook không được kích hoạt hoặc token OAuth hết hạn, Log báo lỗi 401 khi gọi API Shopify
  REJECT: Webhook hoạt động, token còn hiệu lực

Evidence collected (deterministic probes):
[f7512d52-0f1d-4210-8f75-47edef9e28b0] surface=browser: [browser] query: found | value={"title":"dev-quyen-blocker-plus","scripts":[],"httpStatus":200,"frameAncestors":"'none'","appBridgePresent":false,"consoleErrors":["Failed to load resource: the server responded with a status of 404 



Instructions:
1. For EACH hypothesis, produce a verdict (confirmed | rejected | inconclusive).
   - You MUST cite specific evidenceRefs (evidence IDs from above). Do NOT invent evidence.
   - If no evidence supports or refutes a hypothesis → inconclusive.
2. State the root cause: be specific, grounded in confirmed evidence only.
   - If confidence must be low (no confirmed hypothesis), say so explicitly.
3. Confidence: high = 1+ confirmed hypothesis with strong evidence; medium = partial evidence; low = mostly inconclusive.
4. recommendedFix: only if confidence ≥ medium and a clear fix exists.
5. nextSteps: what the developer should investigate or verify manually.

IMPORTANT: ALL text output fields must be in the same language as the Issue. Do not translate code symbols, file paths, or API names.